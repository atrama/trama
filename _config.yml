# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely need to edit after that.
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'jekyll serve'. If you change this file, please restart the server process.

# Site settings
title: Anthony Trama
email: anthony@anthonytrama.com
description: > # this means to ignore newlines until "baseurl:"
  I want to make the web experience better.
baseurl: "/" # the subpath of your site, e.g. /blog
url: "http://anthonytrama.com" # the base hostname & protocol for your site
twitter_username: aftrama
github_username:  atrama
paginate: 7
paginate_path: "blog/page:num"

exclude: [gulpfile.js, Gemfile.lock, node_modules]

gems: [jekyll-paginate, jekyll-assets]


# Build settings
markdown: kramdown

# sass:
#   style: compressed

assets:
  compress:
    css: true
    js: true

  #

  # autowrite: true
  # cache: false | directory | default: .asset-cache
  # cache_type: memory | filesystem | default: filesystem
  # cdn: https://cdn.example.com
  # skip_baseurl_with_cdn: false
  # skip_prefix_with_cdn: false
  # prefix: "/assets"
  # digest: true

  #

  # assets:
  #   - "*.css"
  #   - "*.js"
  #
  # #
  #
  # sources:
  #   - "js/main.js"

  #

  # features:
  #   liquid: true | false | default: false
  #   integrity: true | false | default: false
  #   automatic_img_size: true | false | n(fixnum): 2,4,6,8 | default: true
  #   automatic_img_alt : true | false | default: true
